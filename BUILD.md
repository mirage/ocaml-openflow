# Brief build notes

(to be folded into `README.md`)

To regenerate `oasis` files:

    $ oasis setup-clean
    $ oasis setup

To build:

    $ make configure ## needed to run `mirage configure`
    $ make
